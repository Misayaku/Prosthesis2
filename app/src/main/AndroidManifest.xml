<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="camille.berthelot.prosthesis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.MechaActivity"></activity>
        <activity android:name=".Controller.ControlActivity" />
        <activity android:name=".Controller.HapticActivity" />
        <activity android:name=".Controller.EmgActivity" />
        <activity android:name=".Controller.DebugActivity" />
        <activity android:name=".Controller.UserActivity" />
        <activity android:name=".Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>